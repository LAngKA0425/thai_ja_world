# SPEC_PLATFORM.md — 태자(thai_ja_world) 커뮤니티 플랫폼 운영 정책 & UX 설계

> **버전**: 1.0
> **대상**: 태국 파타야 기반 한인 커뮤니티 — 마사지/생활정보/중고거래/번개모임/후기
> **원칙**: 건전한 생활 정보 공유 플랫폼. 불법 성적 서비스 알선·에스코트 중개 흐름은 절대 포함하지 않는다.
>
> **시스템 아키텍처 참조**: 전체 시스템 구조, 배포 토폴로지, 환경변수 정책, 릴리스 체크리스트는 [`SYSTEM_ARCHITECTURE.md`](./SYSTEM_ARCHITECTURE.md)를 참조하세요.

---

## 목차

1. [커뮤니티 UX 구조](#1-커뮤니티-ux-구조)
2. [신고 시스템](#2-신고-시스템)
3. [관리자 기능](#3-관리자-기능)
4. [정책 엔진](#4-정책-엔진-policypy)
5. [Edge Case 10가지](#5-edge-case-10가지)
6. [관리자 대시보드 UX](#6-관리자-대시보드-ux)

---

## 1. 커뮤니티 UX 구조

### 1-1. 피드 구조

```
/                        ← 메인 피드 (전체 게시글, 최신순)
  ├─ 탭 필터: 전체 | 후기 | 팁 | 장터 | 모임 | 구인
  ├─ 지역 드롭다운: 전체 | 워킹스트리트 | 나클루아 | 중앙파타야 | 졸티엔 | 기타
  └─ 검색 바: 제목+본문 풀텍스트
```

| 요소 | 설명 |
|------|------|
| **정렬** | 기본 `created_at DESC` (최신순). 향후 `hot` (좋아요 가중) 정렬 추가 가능 |
| **페이지네이션** | 커서 기반. `next_cursor` → `created_at` ISO 문자열 |
| **카드 프리뷰** | `[타입 뱃지] [지역] / 제목 / 본문 2줄 / 좋아요·댓글 수` |
| **숨김 글** | `is_hidden=true` → 피드에서 제외. 작성자 본인에게도 비노출 (관리자만 복원 가능) |

**라우트 매핑**

| Frontend Route | API Endpoint | 설명 |
|----------------|-------------|------|
| `/` | `GET /api/v1/posts?type=&area=&tag=&q=&cursor=` | 메인 피드 |
| `/posts/{id}` | `GET /api/v1/posts/{id}` | 게시글 상세 |
| `/write` | `POST /api/v1/posts` | 글 작성 |
| `/search` | `GET /api/v1/posts?q=` | 검색 결과 |
| `/bookmarks` | `GET /api/v1/posts/me/bookmarks` | 내 북마크 |
| `/profile` | `GET /api/v1/auth/me` | 내 정보 |
| `/admin` | 관리자 전용 | 대시보드 |

### 1-2. 게시글 타입 (post.type)

5개 타입. DB enum: `review | tip | market | meetup | job`

#### (A) `review` — 후기

**용도**: 마사지샵 후기, 맛집 후기, 숙소 후기 등 생활 서비스 이용 후기 공유.

| 필드 | 필수 | UI 문구 | DB Column |
|------|------|---------|-----------|
| title | O | "제목" | `posts.title` (String 200) |
| body | O | "내용" | `posts.body` (Text) |
| area | - | "지역" | `posts.area` (String 100) |
| tags | - | "태그 (쉼표 구분)" | `posts.tags` (Text, CSV) |

**UX 흐름**:
1. `/write` → 유형 드롭다운에서 "후기" 선택
2. 제목·내용 입력, 지역·태그 선택적 입력
3. 작성 → 금지 키워드 검사 → 성공 시 피드 리다이렉트
4. 피드에서 `[후기]` 뱃지(인디고)로 표시

**콘텐츠 가이드라인**:
- 구체적 서비스 이용 경험 서술 권장
- 가격, 위치, 서비스 품질 중심 후기
- 연락처 직접 노출은 관리자 재량 허용 (업소 전화번호 등)

#### (B) `tip` — 팁 / 생활정보

**용도**: 비자, 교통, 환전, 병원, 통신 등 파타야 생활 팁.

| 필드 | 필수 | UI 문구 |
|------|------|---------|
| title | O | "제목" |
| body | O | "내용" |
| area | - | "지역" |
| tags | - | "태그" |

**UX 흐름**: `review`와 동일한 작성 폼. 피드에서 `[팁]` 뱃지(청록)로 구분.

**콘텐츠 가이드라인**:
- 정확한 정보 공유 권장 (날짜, 가격, 주소 포함)
- 오래된 정보 업데이트 댓글 환영

#### (C) `market` — 장터 (중고거래)

**용도**: 중고물품 매매, 나눔.

| 필드 | 필수 | UI 문구 | 비고 |
|------|------|---------|------|
| title | O | "제목" | 물품명 + 가격 권장 |
| body | O | "내용" | 상태, 거래 방법 서술 |
| area | O (권장) | "거래 지역" | 거래 장소 |
| tags | - | "태그" | `나눔`, `급매`, `새상품` 등 |

**UX 흐름**:
1. 유형 "장터" 선택
2. 제목에 `[가격]` 포함 권장 (강제 아님)
3. 본문에 물품 사진 URL 또는 설명
4. 피드에서 `[장터]` 뱃지(초록)로 표시

**거래 완료 처리**: 작성자가 글을 수정하여 제목에 `[거래완료]` 추가하거나, 글 삭제.
향후 `is_sold` 필드 추가 고려.

#### (D) `meetup` — 번개모임

**용도**: 식사 번개, 운동 번개, 여행 동행 등.

| 필드 | 필수 | UI 문구 | 비고 |
|------|------|---------|------|
| title | O | "제목" | 모임 주제 |
| body | O | "내용" | 일시, 장소, 인원, 참여 조건 |
| area | O (권장) | "모임 지역" | |
| tags | - | "태그" | `맛집`, `골프`, `낚시` 등 |

**UX 흐름**:
1. 유형 "모임" 선택
2. 본문에 날짜/시간/장소/인원수 자유 서술
3. 참여 의사는 댓글로 표시
4. 피드에서 `[모임]` 뱃지(주황)로 표시

**콘텐츠 가이드라인**:
- 공개 장소 만남 권장
- 참가비 사전 고지 필수 (있을 경우)
- 비용 분담 방식 명시 권장

#### (E) `job` — 구인/구직

**용도**: 아르바이트, 통역, 가이드 등 구인·구직.

| 필드 | 필수 | UI 문구 | 비고 |
|------|------|---------|------|
| title | O | "제목" | 업종 + 조건 요약 |
| body | O | "내용" | 급여, 근무시간, 연락처 |
| area | O (권장) | "근무 지역" | |
| tags | - | "태그" | `통역`, `가이드`, `서빙` 등 |

**UX 흐름**: 기본 작성 폼 동일. 피드에서 `[구인]` 뱃지(보라)로 표시.

**콘텐츠 가이드라인**:
- 급여 조건 명시 필수 (금액 또는 협의)
- 합법적 업종만 허용
- 불법 노동 알선 시 즉시 삭제 + 밴 처리

### 1-3. 게시글 상세 페이지 (`/posts/{id}`)

```
┌──────────────────────────────────┐
│ [후기] 워킹스트리트              │  ← 타입 뱃지 + 지역
│ 마사지샵 A 후기                  │  ← 제목
│ ────────────────────────────     │
│ 본문 전체 노출                   │
│ 태그: #마사지 #추천              │
│ ────────────────────────────     │
│ ♡ 12  💬 3  🔖 북마크  ⚠ 신고  │  ← 액션 바
│ ────────────────────────────     │
│ 댓글 영역                        │
│  유저A: 좋은 정보 감사합니다     │
│  유저B: 가격이 어떻게 되나요?    │
│ ────────────────────────────     │
│ [댓글 입력]              [등록]  │
└──────────────────────────────────┘
```

**액션 바 동작**:

| 액션 | 비로그인 | 로그인 유저 | 작성자 본인 |
|------|---------|------------|------------|
| 좋아요 | 로그인 유도 | 토글 | 토글 |
| 북마크 | 로그인 유도 | 토글 | 토글 |
| 신고 | 로그인 유도 | 신고 바텀시트 | 비노출 (자기 글 신고 불가) |
| 수정 | 비노출 | 비노출 | 수정 페이지 이동 |
| 삭제 | 비노출 | 비노출 | 확인 다이얼로그 후 삭제 |

### 1-4. 댓글 UX

- 단일 뎁스 (대댓글 없음). 간결한 구조 유지.
- 댓글 작성 시 `check_banned_keywords` 적용.
- 댓글 신고: 댓글 옆 `⚠` 아이콘 → 신고 바텀시트.
- 숨김된 댓글: `"관리자에 의해 숨겨진 댓글입니다"` 플레이스홀더 표시.

---

## 2. 신고 시스템

### 2-1. 신고 UX 흐름

```
1. 유저가 게시글/댓글/프로필에서 [⚠ 신고] 탭
2. 바텀시트 오픈 → 신고 사유 선택 (라디오)
3. 선택 사유 + 선택적 상세 설명 입력
4. [신고] 버튼 → POST /api/v1/moderation/reports
5. 토스트: "신고가 접수되었습니다"
6. 같은 대상에 대한 중복 신고 → "이미 신고한 게시글입니다" 토스트
```

**바텀시트 레이아웃**:
```
┌──────────────────────────────────┐
│ 신고하기                         │
│ ────────────────────────────     │
│ ○ 스팸 / 광고                    │
│ ○ 음란 / 불건전 콘텐츠           │
│ ○ 불법 서비스 알선               │
│ ○ 사기 / 허위 정보               │
│ ○ 욕설 / 혐오 표현               │
│ ○ 개인정보 노출                  │
│ ○ 기타                           │
│ ────────────────────────────     │
│ [상세 설명 (선택)]               │
│                                  │
│ [신고하기]                       │
└──────────────────────────────────┘
```

### 2-2. 신고 사유 (report.reason enum)

| reason 값 (DB) | UI 문구 (한국어) | 설명 |
|-----------------|-----------------|------|
| `spam` | 스팸 / 광고 | 반복 홍보, 무관한 광고 |
| `sexual_content` | 음란 / 불건전 콘텐츠 | 노골적 성적 콘텐츠 |
| `illegal_service` | 불법 서비스 알선 | 불법 행위 조장·알선 |
| `fraud` | 사기 / 허위 정보 | 사기 의심, 거짓 정보 |
| `hate_speech` | 욕설 / 혐오 표현 | 비하, 차별, 욕설 |
| `privacy` | 개인정보 노출 | 타인 연락처·사진 무단 게시 |
| `other` | 기타 | 자유 서술 |

**DB 저장**: `reports.reason` 필드에 위 enum 값 저장. 상세 설명은 별도 `reports.detail` 컬럼(Text, nullable) 추가 필요.

### 2-3. 자동 숨김 로직

동일 대상(post 또는 comment)에 신고가 **누적 N건 이상** 도달하면 자동으로 `is_hidden=true` 처리.

| 조건 | 동작 | 설정값 |
|------|------|--------|
| 동일 `target_id`에 신고 >= `AUTO_HIDE_THRESHOLD` (기본 3) | `target.is_hidden = true` | `config.AUTO_HIDE_THRESHOLD` |
| 자동 숨김 시 | Report.status → `"auto_hidden"` | — |
| 관리자가 복원 시 | `is_hidden = false`, 해당 신고들 status → `"dismissed"` | — |

**구현 위치**: `POST /api/v1/moderation/reports` 핸들러 내부에서 신고 생성 직후 카운트 체크.

```
# 의사코드 (policy.py 에 추가)
async def check_auto_hide(db, target_type, target_id):
    count = await count_reports(db, target_type, target_id, status="pending")
    if count >= AUTO_HIDE_THRESHOLD:
        await hide_target(db, target_type, target_id)
        await update_report_status(db, target_type, target_id, "auto_hidden")
```

### 2-4. 중복 신고 방지

- 동일 `reporter_id` + 동일 `target_type` + 동일 `target_id` 조합이 이미 존재하면 → 409 Conflict.
- UI: "이미 신고한 게시글입니다" 토스트.
- DB: `reports` 테이블에 unique constraint: `(reporter_id, target_type, target_id)`.

---

## 3. 관리자 기능

### 3-1. 역할 체계

| role | 권한 |
|------|------|
| `user` | 일반 유저. 글/댓글/좋아요/북마크/신고 |
| `moderator` | user 권한 + 신고 큐 조회, 게시글/댓글 숨김, 유저 임시 정지 |
| `admin` | moderator 권한 + 유저 영구 밴, 키워드 관리, 모더레이터 임명, 대시보드 전체 접근 |

**접근 제어**:
- `require_moderator` → `role in ("moderator", "admin")` (기존 구현 유지)
- `require_admin` → `role == "admin"` (신규 추가 필요)

### 3-2. 신고 큐 (Report Queue)

**라우트**: `GET /api/v1/moderation/reports`

| 필터 | 파라미터 | 설명 |
|------|---------|------|
| 상태 | `status=pending\|resolved\|dismissed\|auto_hidden` | 기본값 `pending` |
| 대상 유형 | `target_type=post\|comment\|user` | 선택 필터 |
| 정렬 | `sort=created_at\|report_count` | 기본 최신순 |
| 페이지네이션 | `cursor`, `limit` | 커서 기반 |

**신고 큐 아이템 표시 정보**:
```
┌────────────────────────────────────────────┐
│ [post] "마사지샵 후기..." — 신고 3건        │
│ 사유: 스팸(1) 불법서비스(2)                 │
│ 최초 신고: 2025-01-15                       │
│ ─────────────────────────────              │
│ [원본 보기] [숨김] [무시] [유저 정지]        │
└────────────────────────────────────────────┘
```

**관리자 액션 흐름**:

| 액션 | API | 결과 |
|------|-----|------|
| 원본 보기 | `GET /api/v1/posts/{id}` 또는 댓글 조회 | 새 탭/모달로 원본 콘텐츠 확인 |
| 숨김 | `POST /api/v1/moderation/hide` | `is_hidden=true`, report status → `resolved` |
| 무시 | `PATCH /api/v1/moderation/reports/{id}` | report status → `dismissed` |
| 유저 정지 | `POST /api/v1/moderation/ban` | 작성자 정지 처리 |

### 3-3. 유저 밴 (Ban/Unban)

**밴 유형**:

| 유형 | `banned_until` | 설명 |
|------|---------------|------|
| 임시 정지 | 미래 날짜 | 해당 날짜까지 로그인 차단 |
| 영구 밴 | `null` + `is_banned=true` | 해제 전까지 영구 차단 |
| 해제 | — | `is_banned=false`, 필드 초기화 |

**밴 유저 접근 시 동작**:
1. 로그인 시도 → 401 + `"계정이 정지되었습니다. 사유: {banned_reason}"`
2. 이미 로그인된 토큰 → API 요청 시 미들웨어에서 밴 상태 체크 → 403
3. 임시 정지 만료 체크: API 요청 시 `banned_until < now()` 면 자동 해제

**밴 사유 예시** (관리자가 자유 입력):
- "반복 스팸 게시"
- "불법 서비스 알선"
- "신고 누적"

### 3-4. 게시글/댓글 숨김

- 기존 `POST /api/v1/moderation/hide` 그대로 활용.
- `hidden=true` → 피드/상세에서 제외.
- `hidden=false` → 복원. 연관 신고 status → `dismissed`.

### 3-5. 키워드 필터

기존 `BannedKeyword` 모델 + CRUD 엔드포인트 활용.

**키워드 카테고리 확장 (향후)**:

| 카테고리 | 예시 | 동작 |
|---------|------|------|
| `block` | 차단 키워드 | 게시 자체 불가 (현재 구현) |
| `flag` | 주의 키워드 | 게시 허용, 신고 큐 자동 등록 |
| `replace` | 치환 키워드 | 자동으로 `***`로 치환 |

현재는 `block` 동작만 구현되어 있으며, `flag`와 `replace`는 `BannedKeyword` 테이블에 `action` 컬럼(String, default `"block"`) 추가로 확장 가능.

---

## 4. 정책 엔진 (`policy.py`)

현재 `policy.py`에는 `check_banned_keywords`만 존재. 아래 규칙들을 단계적으로 추가한다.

### 4-1. 규칙 목록

| # | 함수명 | 트리거 시점 | 설명 |
|---|--------|-----------|------|
| 1 | `check_banned_keywords` | 글/댓글 생성·수정 | **기존 구현**. 금지 키워드 포함 시 차단 |
| 2 | `check_spam_rate_limit` | 글/댓글 생성 | 동일 유저가 짧은 시간에 과도한 글 작성 방지 |
| 3 | `check_duplicate_content` | 글 생성 | 동일/유사 내용 반복 게시 차단 |
| 4 | `check_report_abuse` | 신고 생성 | 단시간 과도한 신고 남발 방지 |
| 5 | `check_banned_status` | 모든 쓰기 API | 밴 유저의 쓰기 행위 차단 |
| 6 | `check_new_account_throttle` | 글/댓글 생성 | 신규 가입 직후 도배 방지 |
| 7 | `check_external_links` | 글/댓글 생성·수정 | 의심스러운 외부 링크 패턴 감지 |

### 4-2. 각 규칙 상세

#### Rule 1: `check_banned_keywords` (기존)
```python
# 현재 구현 그대로 유지
# posts.title + posts.body, comments.body 에서 BannedKeyword 매칭
# 매칭 시 → PolicyViolation 예외 (422)
```

#### Rule 2: `check_spam_rate_limit`
```
조건: 동일 user_id가 최근 N분 내 M건 이상 글 또는 댓글 작성
기본값: 5분 내 5건
저장: Redis key = "rate:{user_id}:post" (TTL 300s, INCR)
위반 시: 429 Too Many Requests + "잠시 후 다시 시도해주세요"
```

#### Rule 3: `check_duplicate_content`
```
조건: 동일 user_id의 최근 24시간 게시글 중 title 또는 body가 80% 이상 유사
방법: 단순 비교 — title 정확 일치 OR body의 앞 200자 일치
위반 시: 409 Conflict + "동일한 내용의 글이 이미 있습니다"
```

#### Rule 4: `check_report_abuse`
```
조건: 동일 reporter_id가 최근 1시간 내 10건 이상 신고
기본값: 1시간 내 10건
저장: Redis key = "rate:{user_id}:report" (TTL 3600s)
위반 시: 429 + "신고 횟수가 초과되었습니다. 잠시 후 다시 시도해주세요"
```

#### Rule 5: `check_banned_status`
```
조건: current_user.is_banned == True AND (banned_until is None OR banned_until > now)
적용: 글 생성, 댓글 생성, 좋아요, 신고 등 모든 쓰기 API
위반 시: 403 Forbidden + "계정이 정지되었습니다"
위치: deps.py의 get_current_user에서 체크 (미들웨어 레벨)
```

#### Rule 6: `check_new_account_throttle`
```
조건: 계정 생성 후 1시간 미만인 유저가 3건 이상 글 작성 시도
목적: 가입 직후 스팸 계정의 도배 방지
위반 시: 429 + "신규 계정은 잠시 후 글을 작성할 수 있습니다"
```

#### Rule 7: `check_external_links`
```
조건: 본문에 외부 링크가 3개 이상 포함된 경우
동작: 차단하지 않되, 자동으로 신고 큐에 플래그 등록
목적: 피싱·스팸 링크 조기 감지
구현: 글 생성 후 비동기(Celery task)로 처리
```

### 4-3. 정책 적용 흐름도

```
유저 요청 (글/댓글 생성)
  │
  ├─ deps.py: get_current_user
  │   └─ check_banned_status ──→ 403 (밴 유저)
  │
  ├─ 라우트 핸들러 진입
  │   ├─ check_spam_rate_limit ──→ 429 (빈도 초과)
  │   ├─ check_new_account_throttle ──→ 429 (신규 계정)
  │   ├─ check_banned_keywords ──→ 422 (금지 키워드)
  │   └─ check_duplicate_content ──→ 409 (중복 콘텐츠)
  │
  ├─ DB 저장 성공
  │
  └─ 비동기 후처리 (Celery)
      └─ check_external_links ──→ 플래그 등록 (신고 큐)
```

---

## 5. Edge Case 10가지

### Edge 1: 자동 숨김 조작 — 조직적 허위 신고

**시나리오**: 특정 유저 그룹이 경쟁 업소 후기에 조직적으로 신고하여 `AUTO_HIDE_THRESHOLD` 도달시킴.

**대응**:
- 같은 IP 대역 또는 동일 시간대 가입 계정에서 동일 대상 신고 시 가중치 감소
- `check_report_abuse`로 단시간 다건 신고 차단
- 관리자가 `dismissed` 처리 시 해당 신고자들의 신고 신뢰도 점수 하락
- 향후 신뢰도 낮은 유저의 신고는 자동 숨김 카운트에서 제외

### Edge 2: 밴 유저의 기존 콘텐츠 처리

**시나리오**: 유저가 밴되었을 때, 이미 작성된 게시글 50건과 댓글 200건은 어떻게 하는가?

**대응**:
- 기본 정책: 기존 콘텐츠는 유지 (정보 가치 보존)
- 영구 밴 시: 관리자가 "콘텐츠 일괄 숨김" 옵션 선택 가능
- API: `POST /api/v1/moderation/ban` 에 `hide_all_content: bool = false` 파라미터 추가
- 일괄 숨김 시 Celery 비동기 처리 (대량 UPDATE)

### Edge 3: 장터 거래 사기 신고

**시나리오**: 중고거래에서 입금 후 잠수. 피해자가 신고하지만 플랫폼은 거래 중개 책임 없음.

**대응**:
- 장터(`market`) 게시글 하단에 고정 안내문 표시: `"태자는 거래 중개를 하지 않으며, 직거래를 권장합니다. 거래 시 주의하세요."`
- 사기 신고(`fraud`) 접수 시 관리자가 해당 유저 프로필에 경고 태그 부여 가능
- 반복 사기 신고 유저 → 자동 플래그 + 관리자 검토 후 밴

### Edge 4: 금지 키워드 우회

**시나리오**: "마 사 지" (띄어쓰기), "마사ㅈ1" (특수문자), "massage" (영문) 등으로 금지 키워드 우회.

**대응**:
- `check_banned_keywords` 확장:
  - 공백/특수문자 제거 후 비교: `normalize(text)` → 공백, `.`, `_`, `-`, 숫자 제거
  - 한영 혼용 패턴: 자주 쓰이는 우회 패턴을 키워드에 직접 등록
  - 초성 분리 검사는 복잡도 대비 효과 낮으므로 1차에서는 제외
- 관리자가 우회 패턴 발견 시 해당 변형도 키워드로 추가 등록

### Edge 5: 번개모임의 노쇼(No-show) 반복

**시나리오**: `meetup` 게시자가 모임을 올려놓고 반복적으로 불참.

**대응**:
- 모임 게시글에 댓글로 "노쇼" 피드백 → 다른 유저들이 판단
- 신고 사유에 `fraud` (허위 정보) 적용
- 동일 유저의 `meetup` 글에 신고 3건 이상 누적 시 관리자 알림
- 플랫폼 레벨의 노쇼 패널티 시스템은 MVP에서 제외 (커뮤니티 자정)

### Edge 6: 동시 신고로 인한 race condition

**시나리오**: 3명이 거의 동시에 같은 글을 신고 → 자동 숨김 트리거가 중복 실행될 수 있음.

**대응**:
- `check_auto_hide` 실행 시 Redis 분산 락 사용
  - Lock key: `auto_hide:{target_type}:{target_id}`
  - TTL: 5초
- DB 레벨에서 `is_hidden` UPDATE는 멱등(idempotent)하므로 중복 실행되어도 데이터 손상 없음
- 신고 카운트는 `SELECT COUNT` 기반이므로 정확한 수치 보장

### Edge 7: 관리자 계정 탈취

**시나리오**: admin 계정이 해킹당해 정상 글 대량 삭제, 유저 무차별 밴.

**대응**:
- admin 역할 변경은 DB 직접 수정만 허용 (API로 role 변경 불가)
- 관리자 액션 전체 감사 로그(audit log) 기록
  - 테이블: `admin_audit_logs (id, admin_id, action, target_type, target_id, detail, created_at)`
  - 모든 hide/unhide, ban/unban, keyword CRUD 기록
- 비정상 패턴 감지: 1시간 내 밴 10건 이상 시 Slack/텔레그램 알림
- 2FA 도입 검토 (향후)

### Edge 8: 자기 글에 자기가 좋아요 도배

**시나리오**: 다중 계정으로 자기 글 좋아요를 조작.

**대응**:
- `PostLike` 테이블의 `(post_id, user_id)` unique constraint로 1인 1좋아요 보장 (기존 구현)
- 다중 계정 감지는 MVP 범위 밖
- 향후: 동일 IP에서 동일 글에 좋아요 3건 이상 시 플래그 등록

### Edge 9: 삭제된 게시글에 대한 신고 처리

**시나리오**: 신고 접수 후 작성자가 글을 삭제 → 관리자가 신고 큐에서 "원본 보기" 클릭 시 404.

**대응**:
- 게시글 삭제 시 실제 DELETE 대신 소프트 삭제 도입 고려
  - `posts.deleted_at` (DateTime, nullable) 컬럼 추가
  - 피드 쿼리: `WHERE deleted_at IS NULL AND is_hidden = false`
  - 관리자 조회: `deleted_at` 무관하게 조회 가능
- MVP에서는 하드 삭제 유지하되, 신고 큐에서 404 시 `"원본이 삭제되었습니다"` 표시
- 해당 신고 status → `"target_deleted"` 자동 업데이트

### Edge 10: 구인 글의 불법 노동 경계선

**시나리오**: "고수익 아르바이트", "여성 우대", "외모 무관하지 않음" 등 모호한 표현의 구인글.

**대응**:
- `job` 타입 게시글에 대한 별도 키워드 감시 목록 운영
  - 키워드 예시: 정책상 구체적 목록은 관리자 내부 운영
  - `flag` 액션으로 등록 → 자동 차단 없이 신고 큐에 플래그
- 구인글 작성 시 안내 문구 표시: `"합법적 업종의 구인만 허용됩니다. 위반 시 게시글 삭제 및 계정 정지됩니다."`
- 관리자 판단 기준:
  - 업종명 명확 → 허용
  - 업종 불명확 + 고수익 강조 → 숨김 후 작성자에게 소명 요청
  - 명백한 불법 → 즉시 숨김 + 밴

---

## 6. 관리자 대시보드 UX

### 6-1. 진입 조건

- `/admin` 라우트. `role`이 `moderator` 또는 `admin`인 유저만 접근.
- 일반 유저 접근 시 → 피드(`/`)로 리다이렉트.
- 네비게이션: 프로필 페이지에서 관리자 역할일 때만 `[관리자]` 링크 노출.

### 6-2. 대시보드 레이아웃

```
┌─ 사이드바 ──┬─ 메인 영역 ─────────────────────────────┐
│             │                                          │
│ 📊 요약     │  ┌── 요약 카드 ───────────────────────┐  │
│ 🚨 신고 큐  │  │ 대기 신고: 12  │ 오늘 신규: 5     │  │
│ 👥 유저 관리 │  │ 자동숨김: 3    │ 정지 유저: 2     │  │
│ 🔑 키워드   │  └────────────────────────────────────┘  │
│ 📈 통계     │                                          │
│             │  [아래에 선택 메뉴별 콘텐츠]              │
└─────────────┴──────────────────────────────────────────┘
```

모바일에서는 사이드바 → 상단 탭 바로 전환 (반응형).

### 6-3. 메뉴별 상세

#### (A) 요약 (Dashboard Home)

**라우트**: `/admin`

| 카드 | 데이터 소스 | API |
|------|-----------|-----|
| 대기 중인 신고 | `reports WHERE status='pending'` COUNT | `GET /api/v1/moderation/stats` (신규) |
| 오늘 신규 신고 | `reports WHERE created_at >= today` COUNT | 동일 |
| 자동 숨김된 콘텐츠 | `reports WHERE status='auto_hidden'` COUNT | 동일 |
| 현재 정지 유저 수 | `users WHERE is_banned=true` COUNT | 동일 |
| 오늘 신규 가입 | `users WHERE created_at >= today` COUNT | 동일 |
| 오늘 게시글 수 | `posts WHERE created_at >= today` COUNT | 동일 |

**신규 API**: `GET /api/v1/moderation/stats` → 위 숫자들을 한 번에 반환.

#### (B) 신고 큐 (Report Queue)

**라우트**: `/admin/reports`

```
┌──────────────────────────────────────────────────────────┐
│ 신고 큐                                                   │
│ ─────────────────────────────────────────────            │
│ 필터: [전체 ▾] [대기중 ▾] [게시글 ▾]                     │
│ ─────────────────────────────────────────────            │
│                                                          │
│ ┌─ 신고 아이템 ────────────────────────────────────────┐ │
│ │ 🔴 post | "워킹스트리트 마사지 후기"                  │ │
│ │ 신고 사유: 불법 서비스 알선 (2건), 스팸 (1건)         │ │
│ │ 신고자: userA, userB, userC                          │ │
│ │ 접수일: 2025-01-15 14:30                             │ │
│ │ ──────────────────────────────────────               │ │
│ │ [원본 보기]  [숨김 처리]  [무시]  [유저 정지]         │ │
│ └──────────────────────────────────────────────────────┘ │
│                                                          │
│ ┌─ 신고 아이템 ────────────────────────────────────────┐ │
│ │ 🟡 comment | "댓글 내용 미리보기..."                  │ │
│ │ 신고 사유: 욕설/혐오 (1건)                            │ │
│ │ ...                                                  │ │
│ └──────────────────────────────────────────────────────┘ │
│                                                          │
│ [더 보기]                                                │
└──────────────────────────────────────────────────────────┘
```

**인터랙션 상세**:

| 버튼 | 클릭 시 동작 |
|------|------------|
| 원본 보기 | 모달로 게시글/댓글 전체 내용 표시. 404 시 `"삭제된 콘텐츠"` 표시 |
| 숨김 처리 | 확인 다이얼로그 → `POST /api/v1/moderation/hide` → 성공 시 해당 아이템 상태를 `resolved`로 변경, 리스트에서 회색 처리 |
| 무시 | 확인 다이얼로그 → `PATCH /api/v1/moderation/reports/{id}` status→`dismissed` → 리스트에서 제거 |
| 유저 정지 | 밴 모달 오픈 → 사유 입력 + 기간 선택 → `POST /api/v1/moderation/ban` |

**밴 모달**:
```
┌──────────────────────────────────┐
│ 사용자 정지                       │
│ ────────────────────────────     │
│ 대상: userA (user@email.com)     │
│ ────────────────────────────     │
│ 정지 유형:                        │
│   ○ 임시 정지                     │
│     기간: [1일 ▾] [3일] [7일] [30일] │
│   ○ 영구 정지                     │
│ ────────────────────────────     │
│ 사유: [                        ] │
│ ────────────────────────────     │
│ □ 해당 유저의 모든 글 숨김 처리    │
│ ────────────────────────────     │
│         [취소]    [정지하기]      │
└──────────────────────────────────┘
```

#### (C) 유저 관리

**라우트**: `/admin/users`

```
┌──────────────────────────────────────────────────────────┐
│ 유저 관리                                                 │
│ ─────────────────────────────────────────────            │
│ 검색: [닉네임/이메일 검색...                ]             │
│ 필터: [전체 ▾] [정지된 유저 ▾]                            │
│ ─────────────────────────────────────────────            │
│                                                          │
│ 닉네임       | 이메일              | 역할  | 상태  | 액션 │
│ ──────────── | ─────────────────── | ───── | ───── | ──── │
│ 파타야김씨   | kim@email.com       | user  | 정상  | [정지] │
│ 스팸봇123    | spam@mail.com       | user  | 정지중| [해제] [상세] │
│ ─────────────────────────────────────────────            │
│ [이전] [다음]                                             │
└──────────────────────────────────────────────────────────┘
```

**신규 API**: `GET /api/v1/moderation/users?q=&status=&cursor=&limit=`
- `q`: 닉네임 또는 이메일 검색
- `status`: `all | active | banned`

**유저 상세 모달**:
- 가입일, 역할, 밴 이력
- 작성 글 수, 받은 신고 수
- 최근 신고 내역 요약

#### (D) 키워드 관리

**라우트**: `/admin/keywords`

기존 관리자 페이지의 키워드 섹션을 독립 페이지로 확장.

```
┌──────────────────────────────────────────────────────────┐
│ 금지 키워드 관리                                          │
│ ─────────────────────────────────────────────            │
│ [키워드 입력...        ] [추가]                           │
│ ─────────────────────────────────────────────            │
│                                                          │
│ 키워드        | 등록일              | 액션                │
│ ───────────── | ─────────────────── | ──────             │
│ 불법키워드1   | 2025-01-10          | [삭제]             │
│ 불법키워드2   | 2025-01-08          | [삭제]             │
│ ...                                                      │
│ ─────────────────────────────────────────────            │
│ 총 23개 키워드                                            │
└──────────────────────────────────────────────────────────┘
```

#### (E) 통계 (향후)

**라우트**: `/admin/stats`

| 지표 | 차트 유형 | 설명 |
|------|---------|------|
| 일별 게시글 수 | 라인 차트 | 타입별 색상 구분 |
| 일별 가입자 수 | 바 차트 | |
| 신고 추이 | 라인 차트 | 사유별 분류 |
| 인기 지역 | 파이 차트 | area 기준 |
| 인기 태그 | 워드 클라우드 | tags 기준 |

MVP에서는 요약 카드만 구현. 차트는 2차 개발.

### 6-4. 관리자 대시보드 라우트 정리

| Frontend Route | 설명 | 권한 |
|----------------|------|------|
| `/admin` | 요약 대시보드 | moderator+ |
| `/admin/reports` | 신고 큐 | moderator+ |
| `/admin/users` | 유저 관리 | moderator+ |
| `/admin/keywords` | 키워드 관리 | admin |
| `/admin/stats` | 통계 (향후) | admin |

### 6-5. 필요한 신규 API 엔드포인트

| Method | Endpoint | 설명 | 권한 |
|--------|----------|------|------|
| `GET` | `/api/v1/moderation/stats` | 대시보드 요약 숫자 | moderator+ |
| `PATCH` | `/api/v1/moderation/reports/{id}` | 신고 상태 변경 (resolved/dismissed) | moderator+ |
| `GET` | `/api/v1/moderation/users` | 유저 목록 (검색/필터) | moderator+ |
| `GET` | `/api/v1/moderation/users/{id}` | 유저 상세 (밴 이력, 신고 내역) | moderator+ |
| `GET` | `/api/v1/moderation/audit-logs` | 감사 로그 조회 | admin |

### 6-6. 신규 DB 테이블/컬럼 요약

| 변경 | 대상 | 설명 |
|------|------|------|
| 컬럼 추가 | `reports.detail` | Text, nullable. 신고 상세 설명 |
| 컬럼 추가 | `banned_keywords.action` | String(20), default `"block"`. 향후 `flag`/`replace` 확장용 |
| Unique 추가 | `reports(reporter_id, target_type, target_id)` | 중복 신고 방지 |
| 테이블 추가 | `admin_audit_logs` | 관리자 액션 감사 로그 |
| 컬럼 추가 (향후) | `posts.deleted_at` | DateTime, nullable. 소프트 삭제 |

---

## 부록: 콘텐츠 정책 요약

### 허용 콘텐츠
- 마사지샵, 맛집, 숙소 등 합법적 생활 서비스 후기
- 비자, 교통, 환전, 병원 등 생활 정보
- 중고물품 직거래
- 식사, 운동, 여행 등 건전한 모임
- 합법적 업종의 구인/구직

### 금지 콘텐츠
- 불법 성적 서비스 알선 또는 에스코트 중개
- 마약 등 불법 물질 거래
- 도박 알선
- 사기, 피싱
- 타인 개인정보 무단 게시
- 혐오 발언, 차별 표현
- 반복적 스팸, 도배

### 제재 단계

| 단계 | 조건 | 조치 |
|------|------|------|
| 1단계 | 최초 경미한 위반 | 해당 콘텐츠 숨김 |
| 2단계 | 2회 위반 | 콘텐츠 숨김 + 경고 메시지 |
| 3단계 | 3회 위반 | 7일 임시 정지 |
| 4단계 | 4회 이상 또는 중대 위반 | 30일 정지 또는 영구 밴 |
| 즉시 밴 | 불법 서비스 알선, 사기 확인 | 영구 밴 + 전체 콘텐츠 숨김 |
